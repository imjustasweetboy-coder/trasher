[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trasher"
version = "1.0.0"
description = "Dev Garbage Devourer by MENOS RUIDO"
readme = "README.md"
requires-python = ">=3.7"

[project.scripts]
trasher = "trasher.cli:main"